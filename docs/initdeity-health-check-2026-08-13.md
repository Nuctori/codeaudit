# InitDeity 体检报告（2026-08-13，引擎 Iter-52-r3 vs Iter-47）

> 对比：旧引擎（2521408，iter47 圈复杂度语义修正）→ 新引擎（00fa06e，iter52-r3——含 iter48-52：图完整度提升/方法组去伪影/反射链/null 抑制/拓扑重载族修复/switch case 复杂度/链式返回表/G1 mutator 守卫）
> 口径：无标注（机器解析面）

## 一、总览对比

| 指标 | Iter-47 | Iter-52-r3 | 变化 |
| --- | --- | --- | --- |
| unknown / rate | 4190 / 14.9% | **4241 / 15.1%** | +51（新形态计数抵消修复收益） |
| chunks | 28060 | 28043 | -17（去伪影） |
| 环 | 83 | **22** | **-61（重载族去伪影——环真实化）** |
| 自环 | 856 | **2450** | +1594（拓扑重载族口径——族内边按自环计） |
| 深度 | 22 | 20 | -2 |
| 传染链（chain≥1） | 1145 | **1220**（chain=3 出现 5 个） | +75 |
| 已知边 / 未知边 | 30770 / 72549 | 29147 / 68702 | 边总数 -5370（去伪影） |
| 函数级 max 复杂度 | Plan C=119 | **OnGUI C=142** | +23（switch case 计数新增——iter53） |
| parseErrors | 77 | 77 | 持平 |

## 二、新引擎健康指标（拓扑新能力——iter50-52）

```
28043 nodes / 29147 edges / 桥比例 18% / 自环 2450 / 环 22 / 深度 20
  ➜ 网状结构（仅 18% 边是唯一通道——多数边有替代路径，环化度高）
  ➜ 2450 自递归 · 22 环（其中 9 个多入口纠缠环——重构雷区）
  ➜ 5175 条桥边（模块间唯一连通通道——契约测试必保接口）
  ➜ 2542 个割点枢纽（必经分量——改动影响面最大）
```

**解读**：

- **网状结构（18% 桥）**：模块间冗余连通——耦合比旧引擎显示的更高（旧只有密度 0.000 的"近树"判断——新桥比例揭示真实网状）
- **9 个多入口纠缠环**：重构雷区（多个调用者进环）——优先解耦
- **2542 割点**：改动影响面最大的必经分量——评审从严清单
- **自环 2450**：重载族口径（族内方法互相调用）——API.g.cs 生成代码是主体

## 三、判定变化（unknown 4190→4241）

| 因素 | 方向 | 说明 |
| --- | --- | --- |
| switch case 复杂度计数（iter53） | + | 新形态（复杂度指标变化，不直接影响 purity） |
| 方法组实参去伪影（iter53） | ± | 裸 identifier 实参位停发 prop-read |
| 链式返回表（iter52-r3） | - | 链式接收者解析 |
| G1 mutator 守卫（iter52-r3） | + | S1 红线——mutate 变异方法守卫 |

净 +51（15.1%）——修复收益被新形态计数部分抵消，但**判定质量提升**（去伪影后边更真实）。

## 四、复杂度 top 更新（新引擎）

| 函数 | C | n | 变化 |
| --- | --- | --- | --- |
| SSUShaderGUI.OnGUI（插件） | **142** | 9 | +14（switch case 计数） |
| DOTweenAnimationInspector.OnInspectorGUI | 119 | 4 | 新入（原 Plan C=119 位） |
| DOTweenAnimation.CreateTween | 111 | 6 | 新入 |
| **RMA.TickOnceAsync** | **103** | 4 | **新发现**（主链 tick——R5 拆分新增目标） |
| SSUShaderGUI.DrawStatus | 75 | 5 | — |
| StarterInteractionCapability.Run | 68 | 3 | — |

**RMA 新增 TickOnceAsync C=103**——自动化主链 tick 循环复杂度首次可见（链式返回表修复后）——R5 拆分目标从 Plan 扩展到 TickOnceAsync。

## 五、治理优先级更新（结合新拓扑）

| 序 | 项 | 新证据 |
| --- | --- | --- |
| R1 | 生成器去重 | 自环 2450 主体（重载族口径下更大） |
| R2 | SDK 枚举包层 | 未变（55 parseError） |
| R3 | **环治理** | 新：9 个多入口纠缠环（优先于全部 22 环）——比旧 83 环更聚焦 |
| R5 | RMA 分解 | 新：TickOnceAsync C=103 + Plan（双复杂函数） |
| R6 | 插件隔离 | 桥比例 18% 揭示：插件连通性被低估 |
| 新 | 割点审计 | 2542 割点——重构前查割点清单（--topology 新能力） |

## 六、结论

- **引擎判定质量提升**：环 83→22（去伪影）、边真实化、复杂度形态补全（switch case）
- **拓扑健康度判断翻转**：旧"近树低耦合"→ 新"网状结构（18% 桥）"——**模块耦合比旧引擎显示的紧密**
- **unknown 15.1%**（+0.2pp）：新形态计数 vs 修复收益的净效应——可接受（方向是判定更精确）
- **治理重点更新**：9 纠缠环 + 割点 2542 + RMA 双复杂函数（Plan + TickOnceAsync）
