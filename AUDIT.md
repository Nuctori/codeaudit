# 32 维交叉审计报告

审计方法：性质测试（随机图对照朴素参考实现）、对抗性输入（畸形/极端/恶意形态）、语言特性矩阵、鲁棒性与工程契约，共 32 个维度、45 个审计用例。全部通过（连同单元与 E2E，总计 73 个测试）。

## 审计中发现的真问题与修复

| # | 发现 | 严重度 | 修复 |
|---|------|--------|------|
| 1 | 同内容重复函数的 `id` 带 `#n` 去重后缀，违反公理4（身份即内容） | 高 | `id` 由内容哈希直接重算，后缀只保留在图键 `key` 上（link.ts） |
| 2 | `resolveSymbol` 只跟随显式 reexport，漏掉 Python `__init__.py` 的转发导入（`from .core import main`） | 高 | 导入跟随不再要求 reexport 标志（Python 语义下模块内绑定即可作为属性访问），深度上限防环 |
| 3 | tree-sitter-python 缩进栈上限约 62 层 | 低（解析器限制） | 非本工具缺陷：超深文件优雅降级为 parseError 占位，不传染其他文件；已固化为测试 |

## 维度清单

### A. 性质测试（随机化对照参考实现）— property.test.ts

1. **Tarjan 正确性**：随机图 SCC 划分与朴素可达性实现逐一相等（分量集合、大小、数量）。
2. **逆拓扑序契约**：跨分量边 u→v ⇒ v 的分量在输出中先于 u 的。
3. **链值正确性**：随机有向图的 chain 与朴素不动点 DP 一致（含环、自环、多种子）。
4. **守恒公理**：每个调用目标要么解析为唯一 chunk 键、要么落入 `?`，无重复无遗漏。
5. **audit/dev 区间语义**：audit 链 ≤ dev 链；区间非零 ⟺ 可达未知；`chainCertain` ⟺ 双跑一致。
6. **排序确定性**：同一输入两次分析输出字节级一致；排序键字典序、不混合量纲。
7. **效应并集传播**：SCC 内效应为成员并集；沿边单调不减。
8. **自环与单子 SCC**：自环不改变纯度结论；单节点无自环不成环。

### B. 对抗性输入 — adversarial.test.ts

9. **语法损坏文件**：半截函数、非法字符 → parseError 占位，其余文件不受影响。
10. **空文件与纯注释文件**：只产生 `<module>` 伪块，统计正确。
11. **极深嵌套**：JS 1000 层不爆栈、nesting 精确；Python 60 层正常，500 层触发解析器上限时优雅降级、不传染。
12. **超长单行**：300KB minified 单行、800 函数构成大环 → SCC 识别、秒级完成。
13. **Unicode/emoji**：中文标识符（PEP 3131）正常建边、chainCertain；emoji 字符串/注释不炸解析器。
14. **同文件大量重复 chunk**：100 个完全相同函数 → key 全唯一，内容 id 全相同（公理4）。
15. **方法遮蔽顶层函数**：同名方法与全局函数按"类内优先、同文件其次"正确归属。
16. **1000 节点大环**：单种子全环 chain=0（SCC 共享语义），秒级完成。
17. **200 函数稠密图**：高扇入扇出不退化，结果与朴素参考一致。
18. **通配符导入地狱**：多层 `import *` / `export *` 交叉，解析与兜底行为确定。
19. **reexport 环**：a→b→c→a 桶文件循环再导出，深度上限终止，不死循环。
20. **自导入**：文件 import 自身，不崩溃、结论确定。

### C. 语言特性矩阵 — lang-features.test.ts

21. **Python**：async/await、装饰器、lambda、嵌套函数、staticmethod、`from . import x`（包内点导入转发）。
22. **TypeScript**：泛型、可选链 `?.`、getter/setter、箭头函数链。
23. **JavaScript**：`require` 改名、解构 require、命名空间调用。
24. **TSX**：泛型组件、JSX 属性中的调用。
25. **动态调用**：`import()` 变量、`require(变量)`、`getattr` 等计入 dynamicCalls，不伪造边。

### D. 鲁棒性与工程 — robustness.test.ts

26. **缓存损坏**：cache.json 被写成垃圾 → 自动回退全量扫描，不崩溃。
27. **缓存失效**：文件内容变化 → 内容哈希失配 → 重扫且结论更新。
28. **文件删除**：缓存中的幽灵条目被清理，不影响结果。
29. **CLI 矩阵**：--help、不存在目录（exit 2）、未知旗标、--top 截断、--strict 退出码 0/1。
30. **三重扫描确定性与性能**：三次扫描字节级一致；300 文件冷扫 < 15s、热扫 < 3s、缓存命中 301 文件。

### E. 输出契约 — contract.test.ts

31. **JSON 模式**：Set→数组、Infinity→"Infinity" 字符串、字段齐全且类型稳定。
32. **文本/unknowns 格式**：分组结构稳定；unknowns 导出含 suggested_prompt；id 为 16 位内容哈希。
